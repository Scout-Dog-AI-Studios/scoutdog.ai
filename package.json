{
  "name": "scoutdog.ai",
  "private": true,
  "version": "1.0.0",
  "description": "Home Page for Scoutdog.ai",
  "scripts": {
    "dev": "turbo run dev --parallel --no-cache",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format"
  },
  "devDependencies": {
    "eslint-config-prettier": "^10.1.2",
    "prettier": "^3.5.3",
    "turbo": "latest"
  },
  "packageManager": "pnpm@10.9.0"
}
